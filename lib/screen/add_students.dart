import 'package:flutter/material.dart';

class ScreenAddStudent extends StatelessWidget {
  const ScreenAddStudent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
